server {
    listen 8080;
    root /home/akosloff/Projects/webserv/webserv;
    index index.html;

    location / 
	{ 
		allowed_methods GET; 
		autoindexing on;
	}

    # temporary workaround for parser
    location /.bla {
        allowed_methods POST;
        cgi_path /home/akosloff/Projects/webserv/webserv/cgi_tester;
    }

    location /post_body {
        allowed_methods POST;
        client_max_body_size 100;
    }

    location /directory/ {
        allowed_methods GET;
        root /home/akosloff/Projects/webserv/webserv/YoupiBanane;
        index youpi.bad_extension;
    }
}